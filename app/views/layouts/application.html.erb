<!DOCTYPE html>
<html>
  <head>
    <title>Bugzilla</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>  <%# this loads Sprockets/Rails asset pipeline %>
    <%= javascript_importmap_tags %> <%#  this loads JS from importmaps %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark row">
      <div class="col"></div>
      <div class="col text-center">
        <%= link_to 'Bugzilla', dashboard_index_path, class: 'navbar-brand display-5 fs-3' %>
      </div>
      <div class="col text-end me-2">
        <%= user_signed_in? ? 
        (button_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-danger position-relative') : 
        (button_to 'Login', new_user_session_path, class: 'btn btn-success position-relative') %>
      </div>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
