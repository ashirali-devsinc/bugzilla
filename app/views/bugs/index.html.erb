<h2 class="text-center">Bugs</h2>
<%= render partial: "shared/index", locals: { obj: @bugs, model: "Bug" } %>
<div class="d-grid">
    <%= (link_to 'Create New Bug', new_bug_path, method: :get, class: 'btn btn-outline-primary btn-block mx-5 mb-2') unless (params[:assign_bug].present?) %>
</div>